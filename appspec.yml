version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/

hooks:
  BeforeInstall:
    - location: /
      timeout: 500
      runas: root
      command: |
        ls -la /var/www/html/
        rm -rf /var/www/html/index.html
        ls -la /var/www/html/
